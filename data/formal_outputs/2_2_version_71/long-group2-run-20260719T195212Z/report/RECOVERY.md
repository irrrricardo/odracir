# Recovery note

`2_19` was recovered from invalid model JSON. `2_13` is a one-page author correction containing no study claims, methods, or results; its empty extraction was audited against the full document and accepted. Successful packets and records were merged into this run. Prior failures remain in `recovery_attempts.jsonl`, and summary usage totals include every attempt.
