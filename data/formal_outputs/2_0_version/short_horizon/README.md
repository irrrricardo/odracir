# Odracir 2.0 short-horizon outputs

No short-horizon Odracir 2.0 formal run has been registered here yet.

When one is created, place it under a group label such as `short_1_6/`, then add a
group manifest and `run-<YYYYMMDDTHHMMSSZ>/run_metadata.json` according to the rules
in `data/formal_outputs/AGENTS.md`.
